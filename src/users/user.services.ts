export default class UserService {
  /**
   * Retrieves a list of users.
   *
   * @return {Array} An array of user objects.
   */
  getUsers() {
    return [
      {
        name: "John Doe",
      },
    ];
  }
  createUser() {}
}
